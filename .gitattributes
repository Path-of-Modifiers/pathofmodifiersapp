# Apply the replaceGraphing filter to all files in the graphing directory
src/frontend/src/hooks/graphing/utils.tsx filter=replaceGraphing
src/frontend/src/hooks/graphing/processPlottingData.tsx filter=replaceGraphing

# Ensure shell scripts use LF line endings
*.sh text eol=lf

# Enforce LF for all text files
* text=auto